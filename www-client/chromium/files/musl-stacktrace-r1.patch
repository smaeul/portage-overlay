diff --git chromium-69.0.3497.100/base/debug/stack_trace.cc
index 7715121..bcdc1a0 100644
--- chromium-69.0.3497.100/base/debug/stack_trace.cc
+++ chromium-69.0.3497.100/base/debug/stack_trace.cc
@@ -214,7 +214,7 @@ const void *const *StackTrace::Addresses(size_t* count) const {
 
 std::string StackTrace::ToString() const {
   std::stringstream stream;
-#if !defined(__UCLIBC__) && !defined(_AIX)
+#if defined(__GLIBC__) && !defined(__UCLIBC__) && !defined(_AIX)
   OutputToStream(&stream);
 #endif
   return stream.str();
diff --git chromium-69.0.3497.100/base/logging.cc
index 7752cce..b531337 100644
--- chromium-69.0.3497.100/base/logging.cc
+++ chromium-69.0.3497.100/base/logging.cc
@@ -585,8 +585,8 @@ LogMessage::LogMessage(const char* file, int line, LogSeverity severity,
 
 LogMessage::~LogMessage() {
   size_t stack_start = stream_.tellp();
-#if !defined(OFFICIAL_BUILD) && !defined(OS_NACL) && !defined(__UCLIBC__) && \
-    !defined(OS_AIX)
+#if !defined(OFFICIAL_BUILD) && !defined(OS_NACL) && defined(__GLIBC__) && \
+    !defined(__UCLIBC__) && !defined(OS_AIX)
   if (severity_ == LOG_FATAL && !base::debug::BeingDebugged()) {
     // Include a stack trace on a fatal, unless a debugger is attached.
     base::debug::StackTrace trace;
